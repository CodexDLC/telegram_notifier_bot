
# Telegram GitHub Notifier Bot

**Telegram GitHub Notifier** — это бот-помощник для команд разработки. Он принимает Webhook-уведомления от GitHub (например, о новых Pull Request'ах), форматирует их в удобный вид и отправляет в специальные **топики (темы)** вашего Telegram-канала или супергруппы.

## 🚀 Возможности

* **Прием Webhook:** Использование **FastAPI** для быстрого и надежного приема данных от GitHub.
* **Безопасность:** Проверка подписи `X-Hub-Signature-256` для защиты от поддельных запросов.
* **Отправка в Telegram:** Использование **aiogram 3.x** для отправки уведомлений.
* **Поддержка топиков:** Умеет отправлять сообщения в конкретные ветки (threads) супергрупп (форумов).
* **Чистая архитектура:** Разделение логики на сервисы и роутеры.

## 🛠 Технологический стек

* **Python 3.12+**
* **FastAPI** (Веб-сервер для хуков)
* **Uvicorn** (ASGI сервер)
* **Aiogram 3** (Telegram API)
* **Loguru** (Логирование)
* **Pydantic** (Валидация данных)

---

## ⚙️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone [https://github.com/YOUR_USERNAME/telegram_bot_.git](https://github.com/YOUR_USERNAME/telegram_bot_.git)
cd telegram_bot_
````

### 2. Создание виртуального окружения

**Windows:**

Bash

```
python -m venv .venv
.venv\Scripts\activate
```

**Linux / macOS:**

Bash

```
python3 -m venv .venv
source .venv/bin/activate
```

### 3. Установка зависимостей

Bash

```
pip install -r requirements.txt
```

### 4. Настройка окружения (.env)

Создайте файл `.env` на основе `.env.example` и заполните его своими данными:

Ini, TOML

```
# Токен вашего Telegram бота (от @BotFather)
BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# Секретный ключ, который вы придумали и указали в настройках Webhook на GitHub
GITHUB_WEBHOOK_SECRET=my_super_secret_key

# ID чата/канала, куда отправлять уведомления (начинается с -100 для супергрупп)
PR_NOTIFY_CHANNEL_ID=-1001234567890

# (Опционально) ID топика (Message Thread ID), если вы используете форумы
# Если не указано, будет отправлено в "Общий" чат.
PR_NOTIFY_TOPIC_ID=2
```

### 5. Запуск сервера

Bash

```
uvicorn main:app --reload
```

- Сервер запустится на `http://127.0.0.1:8000`.
    
- Эндпоинт для Webhook будет доступен по адресу: `http://YOUR_IP/webhook/github`.
    

---

## 🔗 Настройка GitHub Webhook

1. Зайдите в настройки вашего репозитория на GitHub -> **Settings** -> **Webhooks**.
    
2. Нажмите **Add webhook**.
    
3. **Payload URL**: `http://ВАШ_ПУБЛИЧНЫЙ_IP_ИЛИ_ДОМЕН/webhook/github`
    
4. **Content type**: `application/json`
    
5. **Secret**: Тот же ключ, что вы указали в `.env` (`GITHUB_WEBHOOK_SECRET`).
    
6. Выберите события: **Let me select individual events** -> отметьте **Pull requests** (и другие по желанию).
    
7. Нажмите **Add webhook**.
    

---

## 📂 Структура проекта

Plaintext

```
.
├── app/
│   ├── core/          # Конфигурация и настройки (config.py, logging)
│   ├── schemas/       # Pydantic модели (валидация данных от GitHub)
│   ├── services/      # Бизнес-логика (обработка JSON, форматирование текста)
│   └── utils/         # Вспомогательные инструменты
├── main.py            # Точка входа (FastAPI app)
├── requirements.txt   # Зависимости
├── .env               # Переменные окружения (не пушить в гит!)
└── README.md          # Документация
```

---

### ✅ Ваши следующие шаги:

1.  **Создайте репозиторий** на GitHub с названием `telegram_bot_` (или как вы планировали).
2.  **Клонируйте** его себе на компьютер.
3.  **Создайте файлы**:
    * `README.md` (вставьте код выше).
    * `requirements.txt` (вставьте зависимости из прошлого ответа).
    * `.env.example`.
    * `.gitignore` (чтобы не загрузить лишнее, например `.env` и папку `venv`).

**Как только вы будете готовы (папка создана, файлы добавлены), напишите мне "Готово", и мы начнем создавать структуру папок `app/` и писать код!**
